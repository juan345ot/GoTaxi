module.exports = async () => {
  // Espacio reservado para cerrar recursos globales si hiciera falta.
  // Actualmente los recursos se liberan en cada afterAll() individual,
  // pero este archivo se mantiene para posibles extensiones futuras.
};
