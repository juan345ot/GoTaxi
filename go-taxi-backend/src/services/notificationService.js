// Placeholder para futuras integraciones
exports.sendPush = async (userId, title, message) => {
  // Implementar push notifications (OneSignal, FCM, Expo, etc)
  return { success: true };
};

exports.sendWhatsApp = async (telefono, mensaje) => {
  // Integrar Twilio o WhatsApp Business API aquí
  return { success: true };
};